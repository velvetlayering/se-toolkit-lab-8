# PLAN

1. Implement minimal handler + --test entry point.
2. Load secrets via `config.py`.
3. Add `/help`, `/health`, `/labs`, `/scores` handlers.
4. Flesh out services + Telegram wiring in later tasks.
